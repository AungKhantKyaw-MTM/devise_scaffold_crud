
<%= render @friend %>

<div>
  <%= link_to "Edit", edit_friend_path(@friend), class: 'btn btn-secondary' %> |
  <%= link_to "Back", friends_path, class: 'btn btn-secondary' %>
<br/>
  <%= button_to "Destroy", @friend, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger mt-3' %>
</div>
